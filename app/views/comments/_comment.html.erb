<% @blog.comments.each do |comment| %>
  <h4><%= link_to comment.user.username, user_profile_path(id: comment.user.id) %></h4>
  <p><%= time_ago_in_words(comment.created_at) %></p>
  <p><%= comment.comment_entry %></p>
<% end %>
